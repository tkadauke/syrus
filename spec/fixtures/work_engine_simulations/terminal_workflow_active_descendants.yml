name: terminal workflow active descendants
repository:
  owner: simulation
  name: terminal-workflow-active-descendants
jobs:
  job:
    title: Terminal workflow active descendants
    state: failed
    workflow:
      kind: initial
      state: cancelled
      work_unit:
        state: cancelled
      steps:
        - kind: implement
          state: cancelled
          run:
            state: cancelled
        - kind: summarize
          state: running
          run:
            state: running
outcomes:
  default: success
success_states:
  job:
    - failed
    - implemented
