name: merge train unverified member reconciles after land
app_settings:
  merge_train_enabled: true
repository:
  owner: simulation
  name: merge-train-unverified-member-reconciles-after-land
  auto_merge_enabled: true
epics:
  feature:
    title: Feature with unverifiable train member
jobs:
  member:
    title: Member whose landed integration needs reconciliation
    epic: feature
    state: approved
    branch_name: syrus/direct-member
    pr_number: 6901
    approved_at: "2026-09-15 10:00:00"
outcomes:
  default: success
  steps:
    merge_train_land:
      - status: success
        unverified_members:
          - all
expect:
  jobs:
    member:
      - closed
  queues:
    active_runs: empty
    active_work_units: empty
  events:
    - merge_train_land -> {"status" => "success", "unverified_members" => ["all"]
    - repair repair_merge_train_member_reconciliation -> applied
  absent_events:
    - nonretryable_semantic_git_failure
    - rebuild_merge_train
    - stuck
