project:
  id: cli
  label: CLI
  kind: cli

prepare:
  - mise exec go@1.26.5 -- go mod download

grade:
  - type: go-test
    description: |
      Runs the core CLI Go test suite for changes under cli/.
    phases: [review, landing, ci]
    timeout_minutes: 5
