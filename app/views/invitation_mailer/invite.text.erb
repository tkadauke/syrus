<%= @inviter.display_name %> invited you to join Syrus.

Accept the invitation on
<%= new_user_url(token: @invitation.token) %>

<% if @invitation.expires_at.present? %>
This invitation expires on <%= l(@invitation.expires_at, format: :long) %>.
<% end %>
