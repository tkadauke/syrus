<%= t(".invited", inviter: @inviter.display_name) %>

<%= t(".accept_intro") %>
<%= new_user_url(token: @invitation.token) %>

<% if @invitation.expires_at.present? %>
<%= t(".expires", date: l(@invitation.expires_at, format: :long)) %>
<% end %>
