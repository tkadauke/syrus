---
title: Try it locally
description: Run Syrus against any local repo in 60 seconds with a single Docker command.
---

<!-- STUB. Mostly duplicates /evaluate (the top-level CTA page).
     This file is the docs-section version; /evaluate is the
     marketing version. Decide whether to dedupe (canonicalize one,
     redirect the other) or keep both with different framings.

     Implementation issue: bundled with the deployment docs issue.

     Content brief:
     - The 3-step Docker flow from /evaluate
     - Plus: troubleshooting common issues (Docker not installed,
       API key invalid, no internet, etc.)
     - Plus: what's NOT in this path (no real GitHub flow, no PR,
       no persistence between runs)
     - Pointer to Docker Compose for the next step up
-->

# Try it locally

Run Syrus against your own code without installing Ruby, setting up
a database, or configuring GitHub.

<!-- TODO: full content -->
