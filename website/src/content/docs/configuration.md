---
title: Configuration
description: .syrus.yml, per-user settings, per-repo settings.
---

<!-- STUB. Implementation issue: "Docs: configuration + workflows +
     architecture."

     Content brief:
     - .syrus.yml schema with examples (look at the repo's own
       .syrus.yml for reference)
     - Per-user settings: agent provider preference, credentials,
       budget thresholds
     - Per-repo settings: trigger label, default workflow,
       enabled/disabled
     - Env vars used by the worker
     - Secret management
-->

# Configuration

Syrus is configured at three layers: the deployment (env vars and
secrets), the user (per-account settings in the web UI), and the
repository (`.syrus.yml` in the repo).

<!-- TODO: full content -->
