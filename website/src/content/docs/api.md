---
title: API
description: REST API for external systems to create and manage Syrus Jobs.
---

<!-- STUB. Implementation issue: "API reference content" (filed
     after the REST API design issue #196 lands).

     Content brief:
     - Authentication (per-user API tokens)
     - Endpoints: POST /api/v1/jobs, GET /api/v1/jobs/:id,
       GET /api/v1/jobs/:id/logs, POST /api/v1/jobs/:id/cancel
     - Request/response schemas
     - Idempotency via dedup_key
     - Rate limits
     - Webhook adapters (Alertmanager, etc.) — pointer to their
       own dedicated pages
     - OpenAPI spec embed once it exists
-->

# REST API

> **Status: not yet shipped.** The REST API design is tracked in
> [issue #196](https://github.com/tkadauke/syrus/issues/196). This
> page will fill in once the API ships.

<!-- TODO: API reference after #196 lands -->
