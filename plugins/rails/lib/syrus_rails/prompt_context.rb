module SyrusRails
  # Placeholder — injects Rails-specific instructions into the agent system prompt
  # (e.g. generate schema ERD artifact on schema changes).
  # Will be implemented in a follow-up Epic job.
  class PromptContext
  end
end
